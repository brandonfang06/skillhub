apiVersion: v1
kind: Secret
metadata:
  name: skillhub-product-suite-sync-secret
  namespace: skillhub
type: Opaque
stringData:
  # Example only. Rename keys to the private env names read by your internal
  # source module and never commit real credentials.
  PIC_API_TOKEN: change-me
