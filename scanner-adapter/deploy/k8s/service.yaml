apiVersion: v1
kind: Service
metadata:
  name: skill-scanner
  labels:
    app.kubernetes.io/name: skill-scanner
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skill-scanner
  ports:
    - name: http
      port: 8000
      targetPort: http
