# CLI v0.1.8 Upstream Follow-Up Plan

Date: 2026-06-21

## Goal

Bring the local Python-only SkillHub fork's TypeScript CLI package metadata up
to upstream `iflytek/skillhub` `cli-v0.1.8`, without reopening the already
completed upstream app `v0.2.12` behavior port.

## Upstream Release Evidence

- GitHub release: `cli-v0.1.8`
- Release name: `CLI 0.1.8`
- Release URL: <https://github.com/iflytek/skillhub/releases/tag/cli-v0.1.8>
- Published at: `2026-06-18T07:58:44Z`
- Tag commit: `dc185861d4bbc98f1a485e2ddbb5e9199065319f`
- Upstream release body only documents npm/source installation for
  `@astron-team/skillhub@0.1.8` and says to see commit history for changes.

Commands used to confirm scope:

```powershell
git show --no-patch --decorate --oneline v0.2.12
git show --no-patch --decorate --oneline cli-v0.1.8
git log --oneline v0.2.12..cli-v0.1.8
git diff --name-status v0.2.12..cli-v0.1.8
git diff --stat v0.2.12..cli-v0.1.8
```

Observed upstream delta:

```text
dc185861 Merge pull request #542 from iflytek/release/cli-v0.1.8
78b8b34e chore(cli): bump version to 0.1.8

M cli/package.json
M cli/src/generated/pkg-info.ts
```

## Local Baseline Evidence

- Plan-time branch: `dev`
- Plan-time head: `06823d6e feat(release): follow upstream v0.2.12`
- Local reviewed app release: `v0.2.12`
- Local result note:
  `docs/backend-python-maintenance/results/2026-06-20-v0.2.12-intake.md`
- Before this follow-up, local CLI package metadata still reported `0.1.7` in:
  - `cli/package.json`
  - `cli/src/generated/pkg-info.ts`

The `v0.2.12` result note already records the applicable Python backend, CLI
behavior, notification, frontend, release-path, and documentation ports. This
plan only covers the later CLI release tag's package version metadata.

## Gap Summary

| Area | Gap | Action |
| --- | --- | --- |
| Backend Python | None | No backend code or tests should change. |
| Schema/migration | None | No Alembic/baseline SQL work is needed. |
| Frontend/API contract | None | No OpenAPI or web changes are needed. |
| Scanner | None | No scanner changes are in the upstream delta. |
| CLI | Local package metadata remains `0.1.7`; upstream CLI tag is `0.1.8`. | Bump `cli/package.json` and regenerate `cli/src/generated/pkg-info.ts`. |
| Deployment/K8s | None | Do not change compose or K8s manifests. |
| Documentation | Optional | Only update CLI docs if they display the package version explicitly. |
| Tests/verification | Needed | Run CLI metadata generation and CLI gates. |

## Implementation Phases

### Phase 1: Confirm CLI Metadata Generation Path

Steps:

1. Inspect the CLI package scripts and existing tests for generated package
   metadata.
2. Confirm whether `cli/src/generated/pkg-info.ts` is generated by a checked-in
   script and avoid manual edits when a generator exists.

Verify:

```powershell
cd cli
bun test test\unit\scripts\generate-pkg-info.test.ts
```

Success criteria:

- The metadata generation workflow is identified.
- No backend, scanner, frontend, or deployment files are touched.

### Phase 2: Bump CLI Version To 0.1.8

Steps:

1. Update `cli/package.json` from `0.1.7` to `0.1.8`.
2. Regenerate `cli/src/generated/pkg-info.ts` through the repository script.
3. Check whether CLI docs mention the package version; update only if needed.

Verify:

```powershell
cd cli
bun run scripts/generate-pkg-info.ts
bun test test\unit\scripts\generate-pkg-info.test.ts
```

Success criteria:

- `cli/package.json` and generated package info both report `0.1.8`.
- Generated metadata remains consistent with the package file.

### Phase 3: CLI Verification And Result Note

Steps:

1. Run the CLI quality gates.
2. Add a concise result note under
   `docs/backend-python-maintenance/results/2026-07-01-cli-v0.1.8-follow-up.md`
   recording release evidence, changed files, verification output, and
   non-goals.

Verify:

```powershell
cd cli
bun run typecheck
bun run lint
bun run test

cd ..
git diff --check
```

Success criteria:

- CLI typecheck, lint, and tests pass.
- `git diff --check` passes.
- Result note records that this was a CLI-only metadata follow-up.

## Required Tests And Commands

```powershell
cd cli
bun test test\unit\scripts\generate-pkg-info.test.ts
bun run typecheck
bun run lint
bun run test

cd ..
git diff --check
```

## Docs To Update

- Create:
  `docs/backend-python-maintenance/results/2026-07-01-cli-v0.1.8-follow-up.md`
- Update CLI docs only if they explicitly display package version `0.1.7`.

## Execution Result

Status: completed on 2026-07-01.

Changed files:

- `cli/package.json`
- `cli/src/generated/pkg-info.ts`
- `docs/backend-python-maintenance/plans/2026-06-21-follow-upstream-cli-v0.1.8.md`
- `docs/backend-python-maintenance/results/2026-07-01-cli-v0.1.8-follow-up.md`

Outcome:

- `cli/package.json` now reports `0.1.8`.
- `cli/src/generated/pkg-info.ts` was regenerated by
  `bun run scripts/generate-pkg-info.ts` and now reports `0.1.8`.
- CLI docs did not contain a hard-coded `0.1.7` command or install reference,
  so no user-facing CLI docs were changed.
- No backend, schema, frontend runtime, scanner, compose, or K8s files changed.

Verification:

```powershell
cd cli
bun test test\unit\scripts\generate-pkg-info.test.ts
bun run typecheck
bun run lint
bun run test

cd ..
git diff --check
```

Results recorded in:

- `docs/backend-python-maintenance/results/2026-07-01-cli-v0.1.8-follow-up.md`

## Explicit Non-Goals

- Do not reimplement or revisit the already completed `v0.2.12` behavior port.
- Do not change Python backend routes, auth, notifications, schema, or tests.
- Do not change frontend, scanner, compose, K8s, or release runtime behavior.
- Do not reintroduce Java, Maven, Spring Boot, or hybrid runtime assumptions.
- Do not manually edit generated package metadata if the generator can produce
  it.
- Commit and push were explicitly requested for this execution on 2026-07-01.

## Original Paste-Ready Prompt

```text
Work in C:\Users\USER\OneDrive\Documents\skillhub.

Execute docs/backend-python-maintenance/plans/2026-06-21-follow-upstream-cli-v0.1.8.md.

Scope:
- Upstream release/tag: iflytek/skillhub cli-v0.1.8
- Release URL: https://github.com/iflytek/skillhub/releases/tag/cli-v0.1.8
- Published at: 2026-06-18T07:58:44Z
- Upstream delta from v0.2.12 is only the CLI package version bump.

Rules:
- Treat the repo as Python-only; do not reintroduce Java, Maven, Spring Boot, or hybrid runtime assumptions.
- Keep changes scoped to CLI version metadata and any required generated package-info output.
- Do not manually edit generated files when the repository generator can update them.
- Persist a result note under docs/backend-python-maintenance/results/.
- Commit and push only when explicitly approved.

Verification:
cd cli
bun test test\unit\scripts\generate-pkg-info.test.ts
bun run typecheck
bun run lint
bun run test

cd ..
git diff --check
```
