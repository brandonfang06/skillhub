# v0.2.11 Milestone E: Idempotency Cleanup Logging Parity

Date: 2026-06-15

## Scope

Track the upstream `chore(logging)` release item from `v0.2.11`.

Upstream Java change:

```powershell
git diff v0.2.10..v0.2.11 -- server/skillhub-app/src/main/java/com/iflytek/skillhub/task/IdempotencyCleanupTask.java
```

The final upstream tag only changes the cleanup log message so that it includes
the stale-processing threshold instant:

```text
Marked {} stale processing records as failed before threshold={}
```

## Python Decision

No Python code change is required.

The current Python backend still has the `idempotency_record` schema migration,
but it does not have an equivalent idempotency cleanup scheduler, repository
method, or log site. Adding a new scheduler only to mirror this log line would
change runtime behavior beyond the upstream release item.

## Evidence

Search command:

```powershell
rg -n "idempotency|Idempotency|mark_stale|stale.*processing|cleanup" server-python\app server-python\tests
```

Relevant Python matches are limited to schema migrations and unrelated cleanup
paths. There is no Python counterpart for Java
`IdempotencyCleanupTask.markStaleProcessingRecordsAsFailed`.

## Result

- Classification: `verified-no-op`
- Ported code: none
- Follow-up: if Python later implements idempotency middleware and cleanup,
  include the threshold in the cleanup log at that time.
